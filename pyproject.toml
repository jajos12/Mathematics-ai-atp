[project]
name = "mathematics-ai-atp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.12.0",
    "torch-geometric>=2.7.0",
]

[dependency-groups]
dev = ["pytest>=9.0.3"]
